# Raw Vivado readback output: an empty property leaves the trailing tab `emit_readback`
# wrote before it. That is what the tool produced, and tidying it would make this
# evidence a transcription rather than a record. Scoped to this directory.
* -whitespace
