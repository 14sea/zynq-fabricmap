# B2 stage-test correction review — 2026-09-12

Reviewed HEAD: `4f27ffa260afafbe90e7e81716b3161172f2eee5`, initially clean,
three local commits beyond origin/main.

**The original S0-only assertion defect is CLOSED. One P2 remains in the new
committed-state test's S3 invocation.** Correct it before qualification so a later
legal calibration does not force another pinned test change. This review leaves
the production manifest unfrozen; image compatibility remains accepted.

## Corrections accepted

The submitted S0/S1 acceptance script was independently rerun. Production S0
verify/test and the temporary S1 verify/test all pass; production manifest bytes
are unchanged. StageCoverage independently runs **8 tests, zero skips, OK**.
Exact initialization assertions now belong to an explicit S0 fixture, and the
committed test uses production verification followed by stage consistency checks.

Current verified byte hashes:

- Manifest: `848f56528b129939d4d6bfd8d016e455789d958af180033ed668de811a62d140`
- B2 table: `50cb6f5a1d141d18bd011938355f6f900e25dcb40c1e8f2a2aa2e647c8793116`
- Prereg: `68cde86d3f3decaf9775beac94c59731ada486ebe246006e7243156c084db8e0`

Production verify confirms S0, qualified false, no refusal, live image hashing,
B1 chain verification and the 71 B2 / 105 B1 pins. The full reported 449-test run
was not repeated; this review targets the changed transition tests.

## P2: the committed S3 test hard-codes a four-pair slice

`tests/test_b2_runner.py::committed_args` always supplies B2
`pair_first=0, pair_count=4`. The committed test then requires the B2@S3 rejection
to contain `no readable ruling at`. Those expectations agree only when `(0, 4)`
is an actual session in the verified plan.

B2's plan derives its split from the B2Q calibration. Four pairs is one planning
case, not a frozen experiment invariant. The stage tests only use the default
fixture rate 2807/h, which yields exactly that slice and hides this dependency.

Independent reproduction changed only the fixture's documented calibration-rate
input, built each S3 through its normal lifecycle, verified it, and drove the
unchanged committed test through `StageCoverage.drive`:

| Fixture rate (/h) | Valid first slice | S3 verify | Committed test |
|---|---|---|---|
| 2807 | pairs 0–3 | accepted | PASS |
| 602 | pair 0 | accepted | FAIL |
| 4490.86 | pairs 0–6 | accepted | FAIL |
| 6000 | pairs 0–8 | accepted | FAIL |

Each negative result is one failure and zero errors:

```text
'no readable ruling at' not found in
"the slice (0, 4) is not one the plan's split gives"
```

These use the existing fixture's explicit stub qualification evidence and
re-adjudicator. They are test-readiness cases, not measured rates or silicon
qualification. No production verifier/preflight function was replaced. Each plan
is generated by production code and accepted by production verification under
that same documented fixture seam. The runner correctly refuses an invalid
slice; the defect is in the test supplying it.

## Required correction

For an eligible B2@S3 manifest, derive a valid slice from its **verified pinned
plan's actual sessions**, then omit the ruling and assert the missing-ruling
refusal. Do not infer a four-pair plan from a planning rate, weaken the assertion
to accept any refusal, or skip non-four-pair states.

Exercise that same committed-manifest test with the four rates above, preferably
also selecting a non-first session in a split that has one. Retain S0/S1/S2 stage
checks and the illegal-state cases. After editing the pinned test, regenerate
the table and still-unfrozen S0 manifest, and report their replacement hashes.
No firmware, image or prereg edit is needed for this test correction.

The immediate S1 preview now succeeds. The remaining reason to resolve this
before proceeding is that later S3 test failure would require changing pinned
inputs after B2Q, invalidating the strict qualification transition. No new full
449-test execution is required merely to reproduce the finding; the four
direct valid-state controls establish it.

Evidence: [stage/split reproduction](../evidence/b2/review_stage_split_2026_09_12/README.md).
