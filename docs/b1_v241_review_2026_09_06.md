B1 v2.4.1 host correction review — 2026-09-06

**Decision: HOLD. The batch is not ready for push approval or an attempt-2 B1Q ruling
pair.** The previous component-isolation defects and actual-run regression gap are
addressed, but two reproducible evidence-export defects remain.

Reviewed HEAD: `53d9cae`, based on `fd6cf25`, five local commits. The worktree was clean
before this review. Manifest SHA256:
`74a0a447cd7b12592b73621c84976a062f789025324b1e594b33e33dd441fbb2`.
This review used modelled sessions and fake transport only. No board, physical serial,
provisioning, push or ruling operation was performed. The original LOST session was
not modified. Only this report and separate review artifacts were added.

1. **[P2] An incomplete export file table still permits adjudicator PASS.**
   `host/b1_adjudicate.py:119–127` requires six successful statuses but verifies only
   entries present in `files`. It does not require the five physical-file entries.
   Starting with a modelled B1Q session that passes the real adjudicator, remove
   `console.log` and its entry from `exports.json.files`. Keep the existing statuses,
   `complete: true`, and all remaining hashes unchanged. Both `check_exports()` and
   the real B1Q adjudicator accept it, with the latter returning **PASS**. Emptying
   `files` or omitting it entirely also returns PASS. No resealing helper is called.

   Required change: require all five physical files and their hash entries, independently
   of what the document lists. Validate the manifest structure and reject missing or
   malformed entries as REFUSED. Check recorded file metadata consistently with the
   declared schema. Add missing-single-entry, empty-table and absent-table regressions
   through both adjudicators, including removal of a mandatory raw file.

   Scope: the shared check affects B1 and B1Q; the end-to-end PASS reproduction here uses
   B1Q. The later qualification chain separately requires raw files, so this is not a
   demonstrated bypass of the entire qualification verifier. It is an incorrect direct
   adjudicator PASS over an incomplete evidence set.

2. **[P2] Failure to write exports.json escapes the exceptional run path and prevents
   summary.json from being saved.**
   `host/b1_session.py:76–92` can raise while hashing, writing or renaming the export
   manifest. Calls from `export_evidence()` at lines 140 and 215 are unguarded despite
   its "Nothing here raises" contract. In the actual `run()` exception path, the call
   at line 363 can therefore escape `finally` before the summary write at line 378.

   The reproduction invokes the real `run()` with the existing fake-board setup. The
   console loop records a PROTOCOL end and raises a primary host exception. Injecting
   an OSError only at `exports.json.part` writing, or only at its final rename, gives:

   | Result | Write failure | Rename failure |
   |---|---|---|
   | Export error escapes run() | Yes | Yes |
   | summary.json exists | No | No |
   | exports.json exists | No | No |
   | run_log.json and audits.json exist | Yes | Yes |
   | Primary PROTOCOL end survives in run_log.json | Yes | Yes |

   Required change: isolate export-manifest construction and persistence failures,
   retain them as named secondary errors, and independently attempt the final session
   summary write. No adjudication or successful outcome may follow an unsuccessful
   seal. Preserve the primary epoch reason and host exception alongside the seal error.
   Cover the normal finalizer and exceptional run path, including the reader-created
   path before console construction. Account for hashing/read, temporary-write and
   rename failures. This does not require pretending a failed export was complete.

Checks that passed:

- The six required export statuses now gate normal finalization. The earlier raw-write
  failure reproduction is covered, and a faithfully generated incomplete export manifest
  is refused by the adjudicator.
- Independent timing, notary and ledger failures preserve available base records/chunks
  and mark partial artifacts. Summary-construction failure preserves records with a
  null summary and an explicit incomplete marker.
- The new fake-board regressions actually invoke `b1_session.run()`, including a host
  exception after a PROTOCOL end and a loop that exits without an epoch end. This closes
  the previous test gap; the seal failures above are additional uncovered cases.
- B1Q CRC budget remains 4 (noise 2 + forced controls 2), bad-frame budget 2. B1 mapping
  budgets remain 37/37. The committed frozen preregistration, B1 plan/prediction, B1Q
  prediction, firmware source, build script, build evidence and carrier match the
  previously pushed baseline. Local image and ELF match their manifest hashes.
- Manifest refresh reproduces the current manifest. The preregistration hash matches
  its frozen pin; frozen and board_ready are true, qualification remains absent/false.
- All 104 fabricmap pins and 128 instrument pins verify. The instrument worktree remains
  clean at `689dde1`. Every original LOST-session file still matches the session-1
  inspection hash table.
- Independently reran session_finalize, transport, adjudicate, qualification, runner and
  e2e: **92 tests, 0 skips, OK**. The attached log records this run. These passing tests
  do not cover the two counterexamples above.
- Verified all artifact hashes in the committed **1450 tests / 0 skips**, clean-tree
  report `test_report_2026-09-06T083010Z.json`, recorded at `0b3f334`. The full suite was
  not rerun in this review.

Reproduction script, observed JSON and targeted-test log are under
`evidence/b1/review_v241_2026_09_06/`. From the repository root:

```sh
python3 evidence/b1/review_v241_2026_09_06/reproduce_exports.py
```

The script imports existing temporary frozen fixtures and fake-board test helpers.
`require_git=False` permits the local modelled reproduction; the real validators and
B1Q adjudicator execute. It creates separate temporary evidence and does not mutate
committed evidence or access hardware. The seal-failure case uses an empty fake console;
the preservation of 11 records / 88 chunks is covered by the component tests, not this
specific seal-failure injection.

Complete these host fixes, refresh the necessary pins/manifest and clean-tree test
evidence, then submit the resulting batch for review. No firmware change is indicated
by these findings. Keep the LOST attempt and its consumed ruling pair unchanged.
Attempt 2 still requires a reviewed manifest and a fresh ruling pair; none was issued
in this review.
