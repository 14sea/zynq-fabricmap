B1 v2.4.3 lifecycle correction review — 2026-09-06

**Decision: PASS. Approve pushing the six local commits from 98d95d8 through 305335d.**
The qualification-state test defect is closed without relaxing the qualification
transition rule. This is a host review, not an attempt-3 qualification result or an
instruction to execute a board session.

Reviewed HEAD: `305335d6a8b81e08a3dcb9c4227411172a9dec7c`.
Committed manifest SHA256:
`38363973c10c48244dc04f08044647b1159d1446b00dec5776d9478b9aad0a0e`.

1. The committed-manifest tests retain the frozen preregistration byte binding and
   board_ready assertions and permit a standing qualification state. The adjudicator
   test requires PASS when qualified and refusal when unqualified. Explicit-fixture
   negatives still reject an absent record and a bare qualified flag. The earlier failed
   qualified-state report is retained as history rather than presented as passing evidence.

2. Independently ran the production qualified-state checker. It creates a modelled B1Q
   record bound to the committed manifest bytes, pins it through production refresh,
   writes the qualified manifest to the actual test input path and launches a fresh
   unittest process. The independently observed qualified-state result is **1451 ran,
   27 skip events, zero failures/errors, OK**. Both previously state-dependent tests,
   the explicit negative fixtures and the mapping lifecycle regression execute and pass.
   The full verbose log and skip identities are attached. Verbosity was added only to
   the review invocation's unittest command; no pinned source was edited.

3. The 27 skip events are accounted for: 26 individual tests in test_board_carrier_exec
   and one setUpClass skip for test_config_idcode_agreement.TheFourSourcesAgree (five
   tests). Their published-carrier authority guard rejects any dirty tracked tree. These
   tests do not consume the B1 manifest's qualification state. Independently reran all
   **31 affected tests on the restored clean tree: 31 ran, 0 skips, OK**. No B1 test was
   skipped in the qualified-state run. The qualified-state run is not a zero-skip
   clean-tree proof; the combination above is accepted for this lifecycle review.

4. Independently reran runner, adjudicate and qualification in the initial unqualified
   state: **67 ran, 0 skips, OK**. Verified all 18 artifact hashes in the committed
   **1456 ran / 0 skips**, clean-tree report T152151Z at 64d5f0f. The full unqualified
   suite was not rerun in this review. The original passing qualified-state report's
   temporary evidence was still available: independently verified its record and
   reconstructed exactly its pinned manifest digest ce8a5fa3dd9582b139af7039f6749eb674c1b358556f415ef14ab2ef862f74c0.

5. The independent checker run restored the committed manifest bytes and a clean tree.
   Its temporary qualified manifest digest is
   `24440d295c1f70074839c37924d81cbbba1ab28cb8f1345f321cdc0117eae229`.
   That differs from the earlier report because the qualification record includes the
   new temporary evidence path. Both bind the same committed manifest. Neither modelled
   record is a board qualification or is retained in the production manifest.

6. All 105 fabricmap pins and 128 instrument pins verify. The instrument remains clean
   at 689dde1. Firmware, image, ELF, B1 RTL/Vivado inputs, carrier, both plans/predictions,
   frozen preregistration, build evidence and the strict qualification verifier remain
   unchanged from 263928f. Current manifest refresh is identical. All 16 attempt-2
   evidence files still match the independent session-2 audit hash table.

Review artifacts are under evidence/b1/review_v243_2026_09_06/: the binding checks,
targeted log, verbose qualified-state log/report, skip identities and clean-tree rerun
of skipped cases. A preliminary review invocation placed unittest's verbosity argument
before its discover subcommand and failed argument parsing without running tests; that
review invocation was corrected. It is not a product failure or passing test evidence.

Attempt-3 ruling pair issued at the user's request with granted_by 14sea and identifier
2026-09-06-03, board 17A6, session B1Q:

- rulings/b1q_2026-09-06-03.json — whole-of-run B1 carrier qualification, master_seed
  176359248; file SHA256 `78e8f923a96cb699d5a7f74c58f02ebe8944ce59b4582335d716e10c8b2d4d6f`.
- rulings/p3_k_b1q_2026-09-06-03.json — provisioning P3-K; file SHA256
  `82de6b34e381c3dd501310d7548212e24a68bb5929414be38db7e739d538b493`.

Both bind the committed manifest above, image
`300b12b1104b70d1612f4c6236a9280a0556443757b2ddf9dbadd9ef993d5abb`, and frozen preregistration
`f995245cca13d5ac8cba8475c609a6e9f01d269cddc2d87e6a9b980f983652f2`.
Their written JSON was read back and checked with production bind_ruling. Both are
unconsumed. This is a binding check, not a fresh-boundary preflight. Active ruling files
remain only in the ignored rulings directory; review artifacts record their hashes.

Attempt 1 remains LOST; attempt 2 remains a historical PASS for its own manifest and
does not qualify this changed manifest. Qualification remains unpinned/false. After a
standing attempt-3 PASS, pin only the qualification state, commit that transition and
review its exact manifest hash before issuing the B1 mapping pair. Do not change pinned
inputs between that qualification and mapping. Fresh power cycle, boundary within six
hours, full preflight and a separate explicit execution instruction are still required.

The package's stale attempt-2 header and issuance status are corrected in unpinned
documentation. This report, its artifacts and those documentation-only corrections may
be committed and pushed with the reviewed batch while pins/manifest remain unchanged.
No push, provisioning, physical serial access or board session occurred in this review.
