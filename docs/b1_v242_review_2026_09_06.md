B1 v2.4.2 host correction review — 2026-09-06

**Decision: PASS for this host correction review. Approve pushing the seven local
commits from fd6cf25 through 459320d.** Both P2 findings from the v2.4.1 review are
closed. This is not a carrier-qualification PASS or permission to execute a board session.

Reviewed HEAD: `459320d6c3a0ab82aa7d2a4590bb64e590e2d13a`; worktree clean at the start.
Reviewed committed manifest SHA256:
`e38f86a8a7679853eb943f0e968efd880a09faef849606c015ad0ec7616b9709`.

1. **Export completeness: closed.** `check_exports()` requires the declared schema and
   version, exactly six successful statuses and five physical-file entries, complete true,
   and valid per-file status, digest and size matching the actual file. Independently
   replayed all three earlier counterexamples through the real B1Q adjudicator: removing
   console.log and its hash entry, emptying files, and removing files entirely now return
   REFUSED. The untouched modelled baseline still returns PASS. B1 shares this check;
   its regressions also exercise missing entries, malformed metadata and status tables.

2. **Seal failure isolation: closed.** Normal finalization stops before adjudication
   when the seal fails. Independently replayed the real run() exception path with fake
   transport, a primary PROTOCOL end, a console-loop host exception, and an additional
   export-manifest temporary-write or rename error. Neither error escapes run(). Both
   cases save summary.json, run_log.json and audits.json; the summary retains the host
   error and seal error, while the collector summary retains the original PROTOCOL end.
   No success outcome is produced. The rename injection now targets os.replace, matching
   the changed implementation. Tests additionally cover the normal finalizer's hash
   failure and the reader-only path before console construction.

3. **Summary fallback: checked.** Tests cover atomic-write failure followed by a plain
   summary write, and failure of both followed by summary_minimal.json. An independent
   injection of failure in all disk writes reaches stderr and retains the primary reason.
   Consequently, "summary always lands" means independent best-effort persistence with
   a diagnostic fallback; it does not promise a disk file when every disk write fails.

4. **Bindings and retained evidence: checked.** All 104 fabricmap pins and 128 instrument
   pins verify. The instrument remains clean at `689dde1`. Manifest refresh is identical.
   Firmware, B1 RTL/Vivado inputs, build evidence, frozen preregistration, B1 plan and
   prediction, and B1Q prediction match fd6cf25. Local image, ELF and carrier bytes match
   their pins. All 11 original-session files listed in the session-1 inspection still
   match their recorded hashes. No original evidence was rewritten.

5. **Validation: checked.** Independently reran session_finalize, transport, adjudicate,
   qualification, runner and e2e: **98 tests, 0 skips, OK**. Verified all 18 artifact hashes
   in the committed **1456 tests / 0 skips**, clean-tree report
   `evidence/b1/tests/test_report_2026-09-06T114843Z.json`, recorded at `cf59661`.
   The full suite was not rerun in this review. The reproductions and tests use modelled
   sessions or fake transport; they establish no new on-board result.

The independently replayed counterexamples, binding checks and test log are in
`evidence/b1/review_v242_2026_09_06/`. Run the reproduction from the repository root:

```sh
python3 evidence/b1/review_v242_2026_09_06/reproduce_exports.py
```

The script writes separate temporary evidence, imports the existing temporary frozen
fixture and fake-board helper, and executes the real B1Q adjudicator with require_git=False.
It does not open a physical port. The exceptional-run injection has an empty fake console;
the 11-record / 88-chunk preservation checks are separate component regressions.

Attempt-2 ruling pair issued at the user's request, with granted_by `14sea` and identifier
`2026-09-06-02`, for board `17A6`, session `B1Q`:

- `rulings/b1q_2026-09-06-02.json`: whole-of-run B1 carrier qualification, master_seed
  `176359248`; file SHA256 `d07a04f6269268aba3d80f6488e7d9fb73b9bc84949e41e8806ac6c40b2ec386`.
- `rulings/p3_k_b1q_2026-09-06-02.json`: provisioning P3-K; file SHA256
  `1b27ae0c4e758117f8e4e562a244808c150c4dd5243cfd6637cccb2896750869`.

Both bind the reviewed manifest above, frozen preregistration
`f995245cca13d5ac8cba8475c609a6e9f01d269cddc2d87e6a9b980f983652f2`, and image
`300b12b1104b70d1612f4c6236a9280a0556443757b2ddf9dbadd9ef993d5abb`.
The written JSON was read back and checked against the preflight's required identity
fields and production bind_ruling rules; neither has a consumed marker. This was a
binding check, not a full preflight with a fresh boundary record. Active ruling files
remain only in the ignored rulings directory; the review records their hashes.

The first attempt remains LOST/HOLD and its pair remains consumed. Qualification remains
false. The manifest's free-text status still describes the freeze-time state ("no B1Q
session yet"); the package and this review record the actual session history. This wording
is a nonblocking documentation discrepancy, not a change to the structured qualification
state. Its bytes were preserved to keep this review and the new pair bound to the exact
committed manifest.

Before attempt 2: fresh power cycle, a boundary record less than six hours old at go, and
the complete preflight. Execution still requires a separate explicit user instruction.
The existing two-loss / three-without-COMPLETED stop-loss rules remain in force. No push,
runner execution, provisioning, power cycle or physical serial access occurred in this
review. The review/package documentation and review artifacts are left uncommitted;
documentation-only commits containing these changes are also approved for push provided
the reviewed pinned files and manifest remain unchanged.
