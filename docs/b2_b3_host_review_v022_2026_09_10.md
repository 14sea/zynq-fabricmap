# B2 lifecycle fourth review — 2026-09-10

Reviewed HEAD: `8958b2b`, 20 commits ahead of the local `origin/main` reference.
**PASS for the host-only review. Both remaining P2 findings are resolved.** The review
HOLD on advancing this batch and beginning B2 image implementation/build work is closed.
No remaining blocking finding was identified in the reviewed scope. Clearance is limited
to the scope described below; no push or image build was performed by the reviewer.

## Findings closed

1. **Actual image verification.** `_check_frozen_inputs()` now opens the image named by
   the manifest and build evidence, requires its existence, hashes its bytes, and checks
   the declared sizes. The SHA-256 comparison also detects replacement with different
   bytes of the same length. `checks.image` reports the digest prefix and actual size.
   Successful lifecycle fixtures now create real temporary bytes; they do not substitute
   a declaration for the file. The original deletion/replacement counterexamples are
   refused. Truncation, extension, empty files and size-declaration disagreement are
   covered by the focused lifecycle tests.

2. **Complete plan/prediction comparison.** The CLI and verifier share `build_plan()` and
   `build_prediction()`. `canonical_plan()` hashes the gate report against its frozen
   manifest pin before using it. `plan_findings()` cross-checks the frozen experiment,
   engine, map digest, policy and exact seed sequence, then compares the complete generated
   structures recursively. Missing, extra and changed operational fields from the previous
   review are refused by their paths. Only generation time is informational; the omitted
   comparison of `prediction_sha256` is replaced by actual hash checks. S3 requires the
   manifest's prediction reference, the sidecar and the plan's digest to name the same
   bytes. Relocation of an identical prediction remains valid.

The third-review reproducer was rerun **unchanged**: 20 cases, exactly three valid
baselines accepted, all other 17 refused. The second-review reproducer, with its documented
real-image fixture correction, was also rerun: three baselines accepted, all twelve
mutations refused, the slow rate INFEASIBLE, and four invalid rates rejected. These are
offline checks using a fixed adjudicator double, not real B2Q sessions.

## Independent consistency checks

- The canonical plan equals every operational field of the committed plan. Generation
  time and its separately validated prediction digest are excluded from that comparison.
- Prediction generation from an empty cache equals the committed document and produces
  identical file bytes. Deltas remain `[2,5,6,3,4,6,1,-2,5]`; master seed remains 716169644.
- Mutating a returned prediction, including nested run data, does not alter the cached
  prediction or a subsequent result. The cache returns isolated copies.
- A changed generation timestamp with its file hash updated is accepted. Moving the
  manifest's prediction reference to an identical copy is accepted. The prior unrelated
  `{}` prediction counterexample is refused.
- B1's 105 pins verify. Its manifest and qualification implementation remain unchanged,
  as do the B2 gate evidence, prediction and B3 simulation archives in this correction.
  The instrument checkout is clean at `689dde1dad374536c625bbe2b05986ee89eb4c94`.

The reviewer independently ran **105 B2/B3 host tests: OK, zero skips, 244.410 seconds**.
The implementer reports a clean-tree full suite of 1561 tests at `8958b2b`, OK. That is
distinct from this review's independently executed B2/B3 host suite. No new full-suite
report for this HEAD was present in the repository evidence inspected here; this review
does not relabel the focused run as a full-repository clean-tree proof.

## Scope of clearance

The previous decisions remain: D1–D4 accepted for the host design with T/W controls;
gate revisions treated as development revisions; all-self-reporting; N=9 retained;
the silicon experiment framed as prospective reproduction of the fixed prediction.

Closure of the host review permits advancing the reviewed batch and implementing/building
the B2 image under preregistration §7. The resulting firmware, build evidence, C/Python
equivalence, real-application harness, wire/export/adjudication paths and complete pins
must be submitted as the image package. Compatibility review precedes `board_ready` and
freeze; B2Q still needs its own ruling pair and measured calibration before S3.
The standing transport stop-loss requirements remain in effect. This review does not
issue a ruling or authorize board contact, and it does not qualify a B2 image that has
not yet been built. No B3 firmware work is cleared by this B2 lifecycle review.

Artifacts: `evidence/b2/review_v022_2026_09_10/` contains both rerun outputs, the focused
test log, source/hash checks, and `verify_rebuild.py` with its additional positive and
cache-isolation checks. Review additions are English. The reviewer has not changed
production source, original evidence, B1 pins, image or board state, and has not committed
or pushed this review.
