// Consumer-owned synthetic Verilog source for the specimen_attestation 2.0 fixture.
// It is never synthesized; its repository-relative path and hash let the verifier
// independently select the unique .v source from source_build.recipe.sources.
module ff20_recipe_source;
endmodule
