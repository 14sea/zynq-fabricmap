Consumer-owned synthetic recipe input for the specimen_attestation 2.0 known answer.

This is deliberately not a producer builder source. Its only purpose is to give the
conformance fixture a real repository-relative file whose hash the host verifier can
independently recompute.
